# 쇼츠 오토파일럿 — 기능 요약

16:9 가로 강연·설교 영상 하나를 넣으면, 편집 프로그램 없이 **9:16 세로 완성본 쇼츠 MP4**를 자동으로 만들어주는 macOS 데스크톱 앱(pywebview 기반).

## 핵심 파이프라인

1. **전사** — Whisper로 단어·글자 단위 정밀 타임스탬프 추출(한글 토큰 보존).
2. **OREO 기획 (A/B/C 3안)** — 결론(O) → 이유(R) → 예시(E) → 재강조(O) 구조로 원본을 재배열해 쇼츠 대본을 자동 기획.
3. **무음 제거** — 실제로 0.4초 이상 줄어드는 무음만 컷하고, 컷 사이 조각이 1.2초 미만이면 컷을 취소한다. 화면만 튀고 이득 없는 컷을 안 만든다.
4. **컷 인지형 등속 인물 추적 9:16 크롭** — YuNet 모델로 얼굴을 인식하되, 가속/감속 울컥거림 없이 480px/s 등속 패닝 및 제자리 정적 샷 락(Lock) 구성. 비디오 실제 픽셀 차이 피크 프레임 스냅(`snap_cuts_to_visual`) 및 0.5프레임 안전 마진으로 컷 전환 직전 카메라 조기 이동(2단 점프 글리치)을 원천 차단.
5. **영/한 2단 자막 + 줌인 & 초미세 버블팝 SFX** — 10~16자 호흡 단위 자막 분절, 영문 번역, 골드 하이라이트 자막 연동 줌인과 45ms 미니멀 버블팝 효과음 자동 믹싱.
6. **포토제닉 프레임 선별 & 2단 훅(Hook) 썸네일 및 영상 인트로 결합** — 3fps 밀착 인화로 최적 표정 선별 + 인스타그램 3:4 릴스 탭 및 4:5 피드 안전영역(A안) 완벽 대응 2단 훅 타이틀 생성 + 자동 업로더 0초 인식용 무음 썸네일(0.7s) 및 블랙 페이드 트랜지션 자동 결합.
7. **완성본 검증** — 렌더링된 MP4를 재전사해 원래 대본과 대조, 정합성 확인.

## 그 외 기능

- **상시 AI 수정 패널** — 정해진 단계 승인 없이도 화면 우측에서 언제든 자연어로 기획안·자막·대본을 실시간 수정 가능.
- **시스템 진단(Doctor)** — FFmpeg, Whisper CLI, 파이썬 라이브러리, YuNet 모델 설치 상태를 점검하고 설치 가이드 제공.
- **BYO-CLI 지원** — Claude, Gemini, Antigravity 등 사용자가 가진 AI CLI를 붙여 쓸 수 있음(`app/ai_adapter.py`).

## 현재 상태 (README 기준)

- 파이프라인 오케스트레이션과 8개 실행 스크립트(전사·구간추출·무음제거·인물추적·자막·렌더·검증)는 이식·검증 완료.
- `check_chunks.py`(자막-원문 일치 검증)는 아직 오케스트레이션에 미연결 — 자막 확정 전 수동 확인 필요.
- 앱 아이콘·DMG 배경 이미지 미준비 — `.app` 빌드는 가능하나 드래그 설치용 DMG는 배경 이미지 추가 후 가능.
